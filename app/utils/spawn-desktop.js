import { app, BrowserWindow, Menu, shell, screen } from 'electron';

export const spawnDesktop = (Config, TornAPI) => {
  // Coming soon.
  //
  // const {width, height} = screen.getPrimaryDisplay().workAreaSize;
  // var desktopWindow = new BrowserWindow({
  //   "resizable": false,
  //   "type": "desktop",
  //   "skipTaskbar": true,
  //   hasShadow: false,
  //   backgroundColor: "#00ffffff",
  //   transparent: true,
  //   frame: false,
  //   width: width,
  //   height: height
  // });
  // desktopWindow.on('closed', () => {
  //   desktopWindow = null;
  // });
  // desktopWindow.loadURL(`file://${__dirname}/../desktop/desktop.html`);

};
